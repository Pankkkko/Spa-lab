server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ENTRA_ISSUER_URI}
          audiences:
            - ${ENTRA_API_CLIENT_ID}
microservices:
  clientes:
    base-url: http://10.0.3.142:8081
  pedidos:
    base-url: http://10.0.2.157:8082